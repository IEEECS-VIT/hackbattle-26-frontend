import TeamScreen from "@/components/TeamScreen";

export const metadata = {
  title: "Join Team | HackBattle 2026",
};

export default function JoinTeamRoute() {
  return <TeamScreen mode="join" />;
}
